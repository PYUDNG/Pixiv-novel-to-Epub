name: Chrome devtools
version: 0.0.1
schema: v1
mcpServers:
  - name: Chrome devtools
    command: npx
    args:
      - -y
      - chrome-devtools-mcp@latest
      - "--no-usage-statistics"
      - "--browser-url=http://127.0.0.1:9222"
    env: {}
